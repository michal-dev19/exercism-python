"""Modules serves to reverse a string."""


def reverse(text):
    """Reverse a string.

    Args:
        text: A string to reverse.

    Returns:
        The reversed string."""
    return text[::-1]
